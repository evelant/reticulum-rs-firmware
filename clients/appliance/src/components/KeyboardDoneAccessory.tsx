export interface KeyboardDoneAccessoryProps {
  readonly nativeID: string;
}

/** iOS replaces this module with its platform-specific keyboard toolbar. */
export function KeyboardDoneAccessory(_props: KeyboardDoneAccessoryProps) {
  return null;
}
