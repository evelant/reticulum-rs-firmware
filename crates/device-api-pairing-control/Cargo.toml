[package]
name = "reticulum-device-api-pairing-control"
description = "Allocation-free pre-authentication initialization control for the local Reticulum API"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
reticulum-device-api-framing = { path = "../device-api-framing", default-features = false }

[lib]
path = "src/lib.rs"
