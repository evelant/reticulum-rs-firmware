[package]
name = "reticulum-radio-tx-dispatch"
description = "Firmware-includable sole-radio dispatch edge for Reticulum TX"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[features]
default = []

[dependencies]
embassy-sync.workspace = true
embassy-time.workspace = true
rand_core.workspace = true
reticulum-interface-router = { path = "../interface-router", default-features = false }
reticulum-node-core = { path = "../node-core", default-features = false }
reticulum-radio-interface = { path = "../radio-interface", default-features = false }
reticulum-tx-handoff = { path = "../tx-handoff", default-features = false }

[dev-dependencies]
embassy-futures.workspace = true
static_cell.workspace = true

[lib]
path = "src/lib.rs"
