[package]
name = "reticulum-appliance-runtime"
description = "Transport-neutral durable LXMF chat actor and client projections"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[features]
test-support = []

[dependencies]
hex.workspace = true
reticulum-device-api = { path = "../device-api", default-features = false, features = ["lxmf", "nomad", "network-config"] }
reticulum-appliance-sync = { path = "../appliance-sync" }
reticulum-appliance-store = { path = "../appliance-store" }
reticulum-lxmf-wire = { path = "../lxmf-wire" }
personal-rns.workspace = true
serde.workspace = true
tokio.workspace = true
ts-rs.workspace = true
zeroize.workspace = true

[dev-dependencies]
serde_json.workspace = true

[lib]
path = "src/lib.rs"
