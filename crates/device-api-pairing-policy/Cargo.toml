[package]
name = "reticulum-device-api-pairing-policy"
description = "Portable physical-presence and enrollment-window policy for the local Reticulum API"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[features]
default = []

[dependencies]
reticulum-device-api-credentials = { path = "../device-api-credentials", default-features = false }

[lib]
path = "src/lib.rs"
