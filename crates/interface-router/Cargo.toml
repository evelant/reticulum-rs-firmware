[package]
name = "reticulum-interface-router"
description = "Bounded transport-neutral Reticulum outbound interface router"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[features]
default = []

[dependencies]
embassy-sync.workspace = true
reticulum-node-core = { path = "../node-core", default-features = false }

[dev-dependencies]
rand_core.workspace = true
reticulum-rns-rete = { path = "../rns-rete", default-features = false }

[lib]
path = "src/lib.rs"
