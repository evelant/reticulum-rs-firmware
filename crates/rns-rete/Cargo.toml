[package]
name = "reticulum-rns-rete"
description = "Provisional Rete protocol foundation and conformance integration"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[features]
default = []
conformance = []

[dependencies]
reticulum-rns-conformance = { path = "../rns-conformance" }
rete-core.workspace = true
rete-stack = { workspace = true, features = ["alloc"] }
rete-transport.workspace = true
rand_core.workspace = true

[dev-dependencies]
hex.workspace = true
reticulum-radio-interface = { path = "../radio-interface" }
serde_json.workspace = true

[lib]
path = "src/lib.rs"
