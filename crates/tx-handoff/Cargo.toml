[package]
name = "reticulum-tx-handoff"
description = "Bounded owning Embassy channels for Reticulum packet dispatch"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[features]
default = []

[dependencies]
embassy-sync.workspace = true
reticulum-node-core = { path = "../node-core", default-features = false }

[dev-dependencies]
embassy-futures.workspace = true
rand_core.workspace = true
reticulum-rns-rete = { path = "../rns-rete", default-features = false }
static_cell.workspace = true

[lib]
path = "src/lib.rs"
